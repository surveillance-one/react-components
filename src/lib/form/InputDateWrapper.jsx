// import React from "react";
// NOTE: Wrapper for two or more inputs to share the selected date range between
// Component on Hold


// const InputDateWrapper = ({ children }) => {
// 	const handleDate1 = (date) => {

// 	};

// 	const handleDate2 = (date) => {

// 	};

// 	const inputDateChild = (React.Children.map(children, (child, i) => {
// 		if (React.isValidElement(child)) { // if valid element TODO check how to get element name
// 			React.cloneElement(child, props, handleDate1);
// 		}
// 		if (React.isValidElement(child)) { // if valid element TODO check how to get element name
// 			React.cloneElement(child, props, handleDate2);
// 		}
// 	}));

// 	return (
// 		<>
// 			{children}
// 		</>
// 	);
// };
